float a,b,c,d,e;
int main(int a; float b,c) {
    int e, d;
    e = (a * (a-c + d)) * foo(c);
    d = a + b * c - d / e;
    return e + d;
}

int foo() {
    int e, d;
    e = a + b * foo(c);
    d = a + b * c - d / e;
    return e + d;
}

float hello(int a; float b,c) {
    int e, d;
    e = (a + b) * foo(c);
    d = a + b * c - d / e;
    return e + d;
}